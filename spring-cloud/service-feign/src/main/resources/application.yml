server:
  port: 9004
spring:
  application:
    name: spring-feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign:
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health, info, hystrix.stream